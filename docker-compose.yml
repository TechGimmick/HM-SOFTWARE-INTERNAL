services:
  web:
    build: .
    container_name: flask_web_app
    ports:
      - "80:8000"
    restart: always
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - SECRET_KEY=${SECRET_KEY}
      - FLASK_ENV=production
